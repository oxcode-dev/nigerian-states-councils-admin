export type StateFormProp = {
    name: string
    geo_zone: string
    code: string
    capital_city: string
    slogan: string
    description: string
    creation_year: number
    created_at: string
    _id?: string | null
};

export type LgaFormProp = {
    name: string
    geo_zone: string
    code: string
    capital_city: string
    slogan: string
    description: string
    creation_year: number
    created_at: string
    _id?: string | null
};